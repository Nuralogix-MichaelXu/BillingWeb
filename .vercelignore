# 部署到 Vercel 只需要三样东西：
#   app/      静态页面
#   api/      Serverless Function（/__proxy → /api/proxy）
#   vercel.json / package.json
#
# ⚠️ 注意语法：本文件是**黑名单**（与 .gitignore 同为排除语义），
# 即「列在下面的会被排除」，而不是「没列出来的就不上传」。
#
# 而本项目没有配置 outputDirectory。Vercel 的默认行为是：
# 有 public/ 目录就用 public/，没有则回退到项目根目录 `.`。
# 本项目没有 public/ —— 所以**任何未在此列出的根目录内容，
# 都会变成公网可直接下载的资源**（例如 /tests/xxx.test.js）。
# 因此：根目录每新增一项，都必须回来这里确认是否该排除。
# 文档依据：vercel.json 的 outputDirectory 属性，默认值为 public 或 . 根目录。

# 测试套件与本地调试脚本（内部实现细节，绝不外发）
tests/
tools/
server.js

# 设计稿与真机验证截图
*.png

# 仓库与系统元数据
.git/
.gitignore
.vercelignore
.DS_Store
.vercel
*.log
node_modules/

# 本地工作区元数据（记忆笔记等内部开发细节）
.workbuddy/
